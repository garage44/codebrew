pre-commit:
  commands:
    lint-staged:
      run: ./scripts/lint-staged.sh
      env:
        NO_COLOR: 1
        CI: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
